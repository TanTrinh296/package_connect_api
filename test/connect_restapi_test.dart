import 'package:flutter_test/flutter_test.dart';

import 'package:connect_restapi/connect_restapi.dart';

void main() {
  test('adds one to input values', () {
    // RestClient client = RestClient();
    // client.getAsync("https://truyenapi.herokuapp.com", "/movie/");
  });
}
